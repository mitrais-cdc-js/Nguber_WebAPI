@{
  ViewData["Title"] = "Member";
}

<h2>@ViewData["Title"]</h2>
<h3>Edit Member</h3>

<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <form asp-controller="Drivers" asp-action="edit" method="post" class="form-horizontal">
      @{ await Html.RenderPartialAsync("_FormEdit"); }

      <div class="form-group">
        <div class="col-md-offset-4 col-md-8">
          <a asp-area="" asp-action="Index" class="btn btn-default">Back to List</a>
          <button type="submit" class="btn btn-primary"><span class="glyphicon glyphicon-save"></span> Save</button>
        </div>
      </div>
    </form>
  </div>
</div>